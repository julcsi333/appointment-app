export interface Provider {
    id: string;
    name: string;
    businessAddress: string;
    phoneNumber: string;
    imageUrl: string;
}

export interface User {
    id: string;
    name: string;
    phoneNumber: string;
}

